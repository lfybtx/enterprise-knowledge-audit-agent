# Optional. Without this value, the application uses its built-in evidence-only response mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
POSTGRES_DB=enterprise_audit
POSTGRES_USER=audit_app
POSTGRES_PASSWORD=audit_app_dev
DATABASE_URL=postgresql://audit_app:audit_app_dev@db:5432/enterprise_audit
